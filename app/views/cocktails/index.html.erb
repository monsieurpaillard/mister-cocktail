
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-6 col-sm-offset-4">
        <h3><%= link_to "Add a new cocktail", new_cocktail_path %></h3>
        <% @cocktails.each do |cocktail|%>
        <h1><%= link_to cocktail.name, cocktail_path(cocktail) %></h1>
        <%end%>
      </div>
    </div>
  </div>

